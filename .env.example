# Ports exposed on the partner workstation.
FRONTEND_PORT=8080
API_PORT=8000

# This URL is embedded in the frontend image and must be reachable by the browser.
VITE_API_URL=http://localhost:8000

# Evaluation mode only. Microsoft Entra validation is not implemented yet.
AIHUB_AUTH_PROVIDER=demo